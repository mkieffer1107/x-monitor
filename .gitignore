# Rust build artifacts
/target/

# Local app config/state
/x-monitor.toml
/x-monitor-state.json
/logs/

# Monitor config files (keep only the example in git)
/monitor-configs/*
!/monitor-configs/example-account.yaml

# Local secrets and environment files (keep committed examples)
.env
.env.*
!.env.example
!**/.env.example
!/.example.env

# Nested repository clone (kept out of this repo)
/xdk/

# Python test harness (local only)
/python_test/

# OS junk
.DS_Store
